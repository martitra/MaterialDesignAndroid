<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="materialtest.vivz.slidenerd.materialtest">

    <uses-permission android:name="ANDROID.PERMISSION.INTERNET" />
    <application
        android:name=".materialtest.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".materialtest.MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".materialtest.SubActivity"
            android:label="@string/title_activity_sub">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".materialtest.MainActivity" />
        </activity>
        <activity
            android:name=".materialtest.ActivityUsingTabLibrary"
            android:label="@string/title_activity_tab">

        </activity>
    </application>

</manifest>
